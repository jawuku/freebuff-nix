root = true

[*]
charset = utf-8
end_of_line = lf
indent_style = space
indent_size = 2
insert_final_newline = true
trim_trailing_whitespace = true

# Markdown: trailing whitespace can be significant (two spaces = hard line
# break), so leave it alone in prose files.
[*.md]
trim_trailing_whitespace = false
